{% extends "SioSemiBundle::layout.html.twig" %}

{% block content %}
 <h2>Welcome to the Manager:data export : {{seminar.name}}</h2>
 nb inscrits : {{allRegistrations | length }}
 <table class="table table-striped">
   <tbody>
 {% for row in allRegistrations %}
   <tr>
  {% if loop.index0 == 0 %}
    {% for data in row %}
      {% if data.type is not empty %}
         <th class = '{{data.type | e("css")}}'>{{data.name}}</th>
      {% else %}  
         <th>{{data.name}}</th>         
      {% endif %}  
    {% endfor %}
  {% else %}
    {% for data in row %}
      <td>{{data}}</td>
    {% endfor %}
  {% endif %}
   </tr>
   {% endfor %}
 </tbody>
   </table>
 
{% endblock %}
