<!DOCTYPE html>
<html lang="fr">
<head>
{% block head %}
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="Projet Semi">
    <meta name="author" content="Lycée de Léonard de Vinci">
    <link rel="icon" href="../../favicon.ico">

    <title>{{ projectName }}</title>
 {% block stylesheets %}
    <!-- Bootstrap core CSS -->
    <link href="{{ asset('bundles/siosemi/css/bootstrap.min.css') }}" rel="stylesheet">
    <link href="{{ asset('bundles/siosemi/css/semi.css') }}" rel="stylesheet">
{% endblock %}

    <!--[if lt IE 9]>
      <script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
      <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
    <![endif]-->
{% endblock %}
</head>
<body>
{% block body %}
   {# <nav class="navbar navbar-inverse navbar-fixed-top"> #}
     
          {# % if app.user is not null % #}   
            {% include 'SioSemiBundle::menu.html.twig' %}
          {#% endif % #}
     
    <!-- div style="height:5px;"></div -->
    {#
        <div class="container" style="text-align:center; background:#eee">           
                <h3>{{ projectName }}</h3>
                <i>{{ slogan }}</i>
        </div>
    #}
    <div id="container" class="container">
        
        {% for type, flashMessages in app.session.flashbag.all() %}
            {% for flashMessage in flashMessages %}
                <div class="alert alert-{{ type }}" role="alert">
                    {{ flashMessage }}
                </div>
            {% endfor %}
        {% endfor %}
        
        {% block content %}{% endblock %}
        <hr>
        
        <footer>
          <p><center>Projet SioSemi &copy; 2012-{{ "now"|date('Y') }} - lycée Léonard de Vinci - Melun<br><br>              
                <small><small>application de gestion d'inscriptions/réservations à des séances de séminaires (prototype).<br>
                        Vous pouvez retrouver le développement du projet <a href="https://github.com/sio-melun/semi" target="_blank">ici</a>.</small></small></center></p>
        </footer>
    </div>
        
    <script type="text/javascript" src="{{ asset('bundles/siosemi/js/jquery-2.1.3.min.js') }}"></script>
    <script type="text/javascript" src="{{ asset('bundles/siosemi/js/bootstrap.min.js') }}"></script>
    <script type="text/javascript" src="{{ asset('bundles/siosemi/js/semi.js') }}"></script>
{% endblock %}
</body>
</html>